---
import { getEntry, getCollection } from "astro:content";
import { mapStudentCollection } from "../../../utils/student";
import { Image } from "astro:assets";
import CloudinaryImage from "../../ui/CloudinaryImage.astro";
import ClassHeroSection from "../ClassHeroSection.astro";
import StudentCard from "../../StudentCard.astro";
import NextClassesCTA from "../NextClassesCTA.astro";
import FadeOverlay from "../../FadeOverlay.astro";
import StoryTextReveal from "../../sections/StoryTextReveal.astro";

// Import Assets
import tape1 from "../../../assets/washi-tape-strip1.svg?raw";
import tape2 from "../../../assets/washi-tape-strip2.svg?raw";
import tape3 from "../../../assets/washi-tape-strip3.svg?raw";

const tapes = [tape1, tape2, tape3];


// Photos (Using string constants for external URLs)
const group1 = "https://res.cloudinary.com/dhbps9kvp/image/upload/v1778599614/ips3-group1.webp";
const group2 = "https://res.cloudinary.com/dhbps9kvp/image/upload/v1778599611/ips3-group2.webp";
const group3 = "https://res.cloudinary.com/dhbps9kvp/image/upload/v1778599615/ips3-group3.webp";
const group4 = "https://res.cloudinary.com/dhbps9kvp/image/upload/v1778599614/ips3-group4.webp";

const classData = await getEntry("classes", "ips3");
if (!classData) {
  throw new Error("Data kelas ips3 tidak ditemukan di collection");
}
const classContent = classData.data;

const heroBg1 =
  typeof classContent.heroPhoto?.[0] === "string"
    ? classContent.heroPhoto[0]
    : (classContent.heroPhoto?.[0] as any)?.src || "";
const heroBg2 =
  typeof classContent.heroPhoto?.[1] === "string"
    ? classContent.heroPhoto[1]
    : (classContent.heroPhoto?.[1] as any)?.src || "";
const heroBg3 =
  typeof classContent.heroPhoto?.[2] === "string"
    ? classContent.heroPhoto[2]
    : (classContent.heroPhoto?.[2] as any)?.src || "";
const walasPic =
  typeof classContent.walasPhoto === "string"
    ? classContent.walasPhoto
    : (classContent.walasPhoto as any)?.src || "";

// Scattered Memory Collection
const polaroidMemories = [
  { image: group1, date: "SEPT 2023", note: "Morning Circle" },
  { image: group2, date: "DEC 2023", note: "End of Semester" },
  { image: group3, date: "FEB 2024", note: "Creative Session" },
  { image: group4, date: "JUNE 2024", note: "Final Moments" },
];

const studentsRaw = await getCollection(
  "students",
  ({ data }) => data.kelas === "IPS III",
);
const allStudents = mapStudentCollection(studentsRaw);
allStudents.sort((a, b) => a.name.localeCompare(b.name));
---

<div class="overflow-x-hidden bg-[#e5e7eb]">
  <ClassHeroSection className={classContent.className} heroImageSrc={heroBg1} />

  <!-- <FadeOverlay connectorHeight="200" zIndex={10} translateY="-100%" /> -->

  <StoryTextReveal
    walasImage={walasPic}
    walasName={classContent.walasName}
    walasTitle={`Wali Kelas XII ${classContent.className}`}
    topTexts={classContent.walasGreeting.topText}
    bottomTexts={classContent.walasGreeting.bottomText}
    bgImage={heroBg3}
    textColor="text-black"
  />

  <FadeOverlay connectorHeight="40" />

  <!-- Polaroid Wall Section -->
  <section id="ips3-wall-section" class="min-h-screen bg-[#cecfd1] py-32 relative overflow-hidden polaroid-wall-section">
     {/* Wall Grunge Decor */}
     <div class="absolute inset-0 opacity-10 pointer-events-none select-none mix-blend-overlay" style="background-image: url('https://www.transparenttextures.com/patterns/p6.png');"></div>

     <div class="max-w-6xl mx-auto px-6 relative h-[70vh] md:h-[90vh]">
        <div class="absolute inset-0 flex items-center justify-center pointer-events-none z-0">
           <h3 class="text-[18vw] font-black text-white/20 uppercase tracking-widest leading-none select-none italic">JOURNAL</h3>
        </div>

        <div class="relative w-full h-full polaroid-container">
           {
             polaroidMemories.map((memory, index) => (
               <div 
                 class="absolute polaroid-card will-animate opacity-0"
                 style={`z-index: ${index + 10};`}
               >
                  <div class="relative bg-white p-3 pb-8 md:p-4 md:pb-12 shadow-xl transform-gpu">
                    {/* Tape Decoration */}
                    <div class="absolute -top-6 left-1/2 -translate-x-1/2 w-24 h-12 opacity-80 z-20" set:html={tapes[index % tapes.length]} />
                    
                    {/* Photo Container */}
                    <div class="relative aspect-square overflow-hidden bg-gray-100">
                       <CloudinaryImage
                         src={memory.image}
                         alt={`Memory ${index+1}`}
                         class="w-full h-full object-cover grayscale-[20%] hover:grayscale-0 transition-all duration-700"
                         widths={[400, 800]}
                         loading="eager"
                         fetchpriority="high"
                         decoding="async"
                       />
                       {/* <div class="absolute inset-0 z-10 pointer-events-none" set:html={polaroidMask} /> */}
                    </div>

                    {/* Handwritten Note */}
                    <div class="mt-4 md:mt-6 text-center">
                       <p class="font-serif italic text-gray-700 text-lg md:text-xl transform rotate-[1deg]">{memory.note}</p>
                       <span class="block text-[9px] font-mono text-gray-400 mt-1 uppercase tracking-widest">{memory.date}</span>
                    </div>
                  </div>
               </div>
             ))
           }
        </div>
     </div>
  </section>

  <FadeOverlay />

  <!-- Students Grid -->
  <section
    class="students-section py-32 px-6 bg-[#f5f0ea]"
    aria-label={`Daftar siswa kelas ${classContent.className}`}
  >
    <div class="max-w-7xl mx-auto">
      <div class="students-heading mb-24 text-center">
        <p class="font-mono text-[#2A358F]/50 text-xs tracking-[0.6em] uppercase mb-6 opacity-0 students-heading-label will-animate">
          Archived Hearts
        </p>
        <h2 class="text-7xl md:text-9xl font-black text-[#2A358F] uppercase italic tracking-tighter leading-[0.8] opacity-0 students-heading-title will-animate">
          {classContent.className}
        </h2>
        <p class="text-xs text-gray-400 mt-8 opacity-0 students-heading-count will-animate tracking-[0.4em] font-mono">
          [ {allStudents.length} / COLLECTIONS ]
        </p>
      </div>

      <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6 md:gap-8">
        {
          allStudents.map((student) => (
            <div class="student-card-wrapper opacity-0 will-animate">
               <StudentCard
                name={student.name}
                slug={student.slug}
                photo={student.photo}
                quote={student.quote}
                class={student.class}
                classSlug="xii-ips-3"
              />
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <FadeOverlay />

  <NextClassesCTA
    prevClass={classContent.prevClass}
    nextClass={classContent.nextClass}
    customNext={classContent.customNext}
    showBackLink={false}
  />

  <style>
    body {
      @apply bg-[#e5e7eb];
    }
    .polaroid-card {
      width: 240px;
      @media (min-width: 768px) {
        width: 300px;
      }
    }
  </style>

  <script>
    import { gsap } from "gsap";
    import { ScrollTrigger } from "gsap/ScrollTrigger";
    import { waitForLoading } from "../../../utils/loadingState";

    gsap.registerPlugin(ScrollTrigger);

    document.addEventListener("astro:page-load", async () => {
      const ctx = gsap.context(() => {
        // --- INITIAL STATE / FOUC PREVENTION ---
        gsap.set(".students-heading-label, .students-heading-title, .students-heading-count", { opacity: 0, y: 30 });
        gsap.set(".student-card-wrapper", { opacity: 0, y: 50 });
      });

      await waitForLoading();

      ctx.add(() => {
        const section = document.querySelector("#ips3-wall-section");
        if (section) {
          // --- POLAROID WALL ANIMATIONS ---
          const cards = gsap.utils.toArray<HTMLElement>(".polaroid-card", section as HTMLElement);
          const positions = [
            { top: "20%", left: "10%", rotate: -8 },
            { top: "15%", left: "85%", rotate: 12 },
            { top: "52%", left: "38%", rotate: 6 },
            { top: "56%", left: "62%", rotate: -10 },
          ];

          cards.forEach((card, i) => {
            const pos = positions[i % positions.length];
            gsap.set(card, {
              top: pos.top,
              left: pos.left,
              xPercent: -50,
              yPercent: -50,
              rotation: pos.rotate,
              opacity: 1
            });
          });

          const tl = gsap.timeline({
            scrollTrigger: {
              trigger: section,
              start: "top top",
              end: "+=150%",
              pin: true,
              scrub: 1.2,
              anticipatePin: 1
            }
          });

          cards.forEach((card, i) => {
            tl.from(card, {
              y: () => window.innerHeight,
              x: (i % 2 === 0 ? -150 : 150),
              rotation: (Math.random() - 0.5) * 60,
              scale: 0.7,
              opacity: 0,
              duration: 2,
              ease: "power2.out",
              force3D: true
            }, i * 0.45);
          });
        }

        // --- STUDENTS HEADING ---
        gsap.to(".students-heading-label, .students-heading-title, .students-heading-count", {
          opacity: 1,
          y: 0,
          duration: 1.5,
          stagger: 0.15,
          ease: "expo.out",
          force3D: true,
          scrollTrigger: {
            trigger: ".students-section",
            start: "top 85%",
            once: true
          }
        });

        // --- STUDENT CARDS (Batch Optimized) ---
        ScrollTrigger.batch(".student-card-wrapper", {
          onEnter: (batch) => {
            gsap.to(batch, {
              opacity: 1,
              y: 0,
              duration: 1,
              stagger: 0.1,
              ease: "power2.out",
              force3D: true
            });
          },
          start: "top 95%",
          once: true
        });
      });

      // Cleanup
      document.addEventListener("astro:before-swap", () => {
        ctx.revert();
      }, { once: true });
    });
  </script>
</div>
